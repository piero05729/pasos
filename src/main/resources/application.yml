
server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pasos?serverTimezone=America/Lima&useSSL=false
    username: root
    password: piero291205
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

